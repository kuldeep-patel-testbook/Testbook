{
  "name": "patient-scheduler",
  "version": "1.0.0",
  "description": "Hospital Patient Scheduler Module - MERN Stack",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": ["patient", "scheduler", "hospital", "mern", "appointment"],
  "author": "Hospital Management System",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}